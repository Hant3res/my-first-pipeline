name: .NET CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: 💡 Сборка и тестирование
    runs-on: ubuntu-latest
    
    steps:
    # Шаг 1: Получаем код из репозитория
    - name: 💡 Загружаем код
      uses: actions/checkout@v4
    
    # Шаг 2: Устанавливаем .NET
    - name: 💡 Устанавливаем .NET 8
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.x'
    
    # Шаг 3: Восстанавливаем зависимости для основного проекта
    - name: 💡 Восстанавливаем пакеты для Calculator
      run: dotnet restore Calculator/Calculator.csproj
    
    # Шаг 4: Восстанавливаем зависимости для тестового проекта  
    - name: 💡 Восстанавливаем пакеты для тестов
      run: dotnet restore Calculator.Tests/Calculator.Tests.csproj
    
    # Шаг 5: Собираем основной проект
    - name: 💡 Собираем Calculator
      run: dotnet build Calculator/Calculator.csproj --no-restore --configuration Release
    
    # Шаг 6: Запускаем тесты
    - name: 💡 Запускаем тесты
      run: dotnet test Calculator.Tests/Calculator.Tests.csproj --no-build --verbosity normal
